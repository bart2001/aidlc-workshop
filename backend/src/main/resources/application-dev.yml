spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    hikari:
      leak-detection-threshold: 60000

logging:
  level:
    com.tableorder: DEBUG
    org.springframework.security: DEBUG
